#include <cstdlib>
#include <iostream>

#include "node.h"

using namespace std;

int main(int argc, char *argv[])
{
    struct queue *p;
    init(p);
    system("PAUSE");
    return EXIT_SUCCESS;
}
